apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: backend-services
  namespace: shop-maram
spec:
  ingressClassName: nginx
  rules:
    - host: 48.193.24.54.nip.io
      http:
        paths:
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: backend-service
                port:
                  number: 3001
